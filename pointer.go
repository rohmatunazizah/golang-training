package main

// import "fmt"

// func pointer(){
	// var name string = "john"
	// var nameAddress *string = &name

	// fmt.Println("name (value) :", name)
	// fmt.Println("name (address) :", &name) //mengambil address dari var yang di pointing
	// fmt.Println("nameAddress (value) :", *nameAddress) //mengambil value dari address yang kita pointing
	// fmt.Println("nameAddress (address) :", nameAddress)

	// name =  "Doe" 

	// fmt.Println("")
	// fmt.Println("name (value) :", name)
	// fmt.Println("name (address) :", &name) 
	// fmt.Println("nameAddress (value) :", *nameAddress) 
	// fmt.Println("nameAddress (address) :", nameAddress)

	// number_a := 25
	// var number_b *int
	// if number_b == nil{
	// 	fmt.Println("number_b is", number_b)
	// 	number_b = &number_a
	// 	fmt.Println("number_b after init : is", *number_b)
	// }

	// var size = new(float64)
	// fmt.Printf("Size value is : %f \n", *size)
	// fmt.Printf("Type is : %T \n", size)
	// fmt.Printf("Address is : %v  \n", &size)
	// *size = 85

	// fmt.Println("New size (value) is :", *size)

	

// }